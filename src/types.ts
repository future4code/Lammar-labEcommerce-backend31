export type userType = {
    id: string
    name: string
    email: string
    password: string
}

export type productType = {
    id: string
    name: string
    price: number
    image_url: string
}